# mcp-memory

This is an MCP server that provides two tools:

- `recall`: recalls _memories_ in order similarity to a given input query
- `capture`: persists a _memory_.

## Authentication

Authentication is powered by clerk.

## Authorization

Any authorized user may use the tools.

Data is namespaced by user id.